<!DOCTYPE html>
<html lang="en">
  <title> </title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://unpkg.com/tachyons/css/tachyons.min.css">
  <body>

    <main class="pa4 black-80">
      <form method="POST" class="measure center">
        <fieldset id="sign_up" class="ba b--transparent ph0 mh0">
          <legend class="f4 fw6 ph0 mh0">ログイン</legend>
          {% if login_error %}
          <div class="mt1 red">メールアドレスまたはパスワードが間違っています
          </div>
          {% endif %}
          <div class="mt3">
            <label class="db fw6 lh-copy f6">メールアドレス</label>
            <input class="pa2 ba bg-transparent w-100" type="text" name="email_address" value="{{email_address}}">
          </div>
          <div class="mv3">
            <label class="db fw6 lh-copy f6">パスワード</label>
            <input class="b pa2 ba bg-transparent w-100" type="password" name="password" value="{{password}}">
          </div>
        </fieldset>
        <div class="">
          <input class="b ph3 pv2 input-reset ba b--black bg-transparent grow pointer f6 dib" type="submit" value="ログイン">
        </div>
        <div class="lh-copy mt5">
          <a href="/pw_reset/start" class="f5 link dim black db">パスワードをお忘れですか</a>
        </div>
      </form>
    </main>

  </body>
</html>
